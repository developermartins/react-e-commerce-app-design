import React from 'react';
import styled from 'styled-components';

const CatgoryItem = ({ item }) => {
  return (
    <Container>CatgoryItem</Container>
  );
};

const Container = styled.section``;

export default CatgoryItem;
